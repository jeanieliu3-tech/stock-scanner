services:
  - type: web
    name: stock-scanner
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: GIN_MODE
        value: "release"
